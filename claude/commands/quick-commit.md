---
description: "Stage all changes and commit with a descriptive message"
---

1. Run `git status` to see the current state
2. Run `git diff` to understand the changes
3. Stage all changes with `git add -A`
4. Create a commit with a clear lowercase message that briefly describes what changed, using imperative mood ("add feature" not "added feature")

Example: `add product search functionality`
